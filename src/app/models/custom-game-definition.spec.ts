import { CustomGameDefinition } from './custom-game-definition';

describe('CustomGameDefinition', () => {
  it('should create an instance', () => {
    expect(new CustomGameDefinition()).toBeTruthy();
  });
});
